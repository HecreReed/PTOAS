# `01 算子复现部署` 指标

默认把 PTOAS 看作“从样例或 `.pto` 输入出发，完成构建、编译、compile-only 或上板验证”的工具链仓库。

## 默认任务模板

若用户没有指定具体 case，优先用 `test/samples/MatMul/` 作为复现模板：

```bash
python3 test/samples/MatMul/tmatmulk.py > /tmp/tmatmulk.pto
./build/tools/ptoas/ptoas /tmp/tmatmulk.pto -o /tmp/tmatmulk.cpp
```

需要无卡 compile-only 时，继续参考：

```bash
python3 test/npu_validation/scripts/generate_testcase.py \
  --input /tmp/tmatmulk.cpp \
  --run-mode npu \
  --soc-version Ascend910
```

## 易学习

### 文档获取

| 指标 | 在 PTOAS 中怎么测 | 主要证据 | 评分口径 |
| --- | --- | --- | --- |
| 检索命中成功率 | 统计找到“构建 + sample 运行 + compile-only/上板验证”入口所需检索轮次 | `README.md`, `docs/no_npu_compile_only_guide_zh.md` | 1 次 `(8,10]`；2 次 `(6,8]`；3 次 `(4,6]`；4-5 次 `(2,4]`；>5 次 `[0,2]` |
| 文档检索耗时 | 从开始找文档到定位到正确路径的时间 | 同上 | `<=2` 分钟 `(8,10]`；`2-4` `(6,8]`；`4-6` `(4,6]`；`6-8` `(2,4]`；`>8` `[0,2]` |
| 文档获取成功率 | 目标文档是否都能在仓库内找到 | 同上 | `100%` `(8,10]`；`80%-99%` `(6,8]`；`60%-79%` `(4,6]`；`40%-59%` `(2,4]`；`<40%` `[0,2]` |

### 文档学习

| 指标 | 在 PTOAS 中怎么测 | 主要证据 | 评分口径 |
| --- | --- | --- | --- |
| 文档错误点位密度 | 按文档执行时，检查命令、路径、版本、链接、说明是否错误 | `README.md`, `docs/no_npu_compile_only_guide_zh.md`, `.github/workflows/ci.yml` | 零错误 `(8,10]`；少量小错 `(6,8]`；一般错误 `(4,6]`；明显错误 `(2,4]`；严重不可用 `[0,2]` |
| 文档跳转次数 | 从首个命中文档到凑齐完整执行路径，跨文档跳转的次数 | `README.md` -> `docs/...` -> `test/samples/...` | 1 次 `(8,10]`；2 次 `(6,8]`；3 次 `(4,6]`；4-5 次 `(2,4]`；>5 次 `[0,2]` |
| 内容覆盖缺失率 | 是否缺少无卡编译、sample 运行、上板验证、A3/A5 差异等关键场景 | `README.md`, `docs/...`, sample README | 无缺失 `(8,10]`；`<=5%` `(6,8]`；`5%-15%` `(4,6]`；`15%-30%` `(2,4]`；`>30%` `[0,2]` |
| 文档学习耗时 | 从开始读到能给出执行方案的时间 | 同上 | `<=5` 分钟 `(8,10]`；`5-7` `(6,8]`；`7-9` `(4,6]`；`9-11` `(2,4]`；`>11` `[0,2]` |
| 通过文档学习检索成功率 | 需要通过文档解决的问题里，有多少真正被文档回答 | 同上 | `100%` `(8,10]`；`80%-99%` `(6,8]`；`60%-79%` `(4,6]`；`40%-59%` `(2,4]`；`<40%` `[0,2]` |

## 易部署

### 环境下载

| 指标 | 在 PTOAS 中怎么测 | 主要证据 | 评分口径 |
| --- | --- | --- | --- |
| 环境获取场景覆盖率 | 看仓库是否说明源码构建、无卡 compile-only、上板验证所需环境 | `README.md`, `docs/no_npu_compile_only_guide_zh.md` | `100%` `(8,10]`；`80%-99%` `(6,8]`；`60%-79%` `(4,6]`；`40%-59%` `(2,4]`；`<40%` `[0,2]` |
| 环境下载耗时 | 真实下载 LLVM/CANN/Python 依赖/pto-isa 的时间 | `README.md`, `ci.yml` | `<=5` 分钟 `(8,10]`；`5-7` `(6,8]`；`7-9` `(4,6]`；`9-11` `(2,4]`；`>11` `[0,2]` |
| 环境下载成功率 | 下载步骤是否一次完成 | 真实执行记录 | `100%` `(8,10]`；`80%-99%` `(6,8]`；`60%-79%` `(4,6]`；`40%-59%` `(2,4]`；`<40%` `[0,2]` |

说明：如果当前任务没有真的去下载 LLVM/CANN/`pto-isa`，后两项写 `未实测`，不要脑补成功率。

### 环境安装

| 指标 | 在 PTOAS 中怎么测 | 主要证据 | 评分口径 |
| --- | --- | --- | --- |
| 软硬件配套兼容率 | 检查仓库是否明确 LLVM 版本、Python 包版本、CANN / `pto-isa` 依赖关系 | `README.md`, `docs/no_npu_compile_only_guide_zh.md`, `ci.yml` | `100%` `(8,10]`；`80%-99%` `(6,8]`；`60%-79%` `(4,6]`；`40%-59%` `(2,4]`；`<40%` `[0,2]` |
| 部署时长 | 从开始执行安装到可运行 `ptoas`/Python 绑定的时间 | 真实执行记录 | `<=5` 分钟 `(8,10]`；`5-7` `(6,8]`；`7-9` `(4,6]`；`9-11` `(2,4]`；`>11` `[0,2]` |
| 操作步骤数 | 完成安装所需命令/步骤数量 | `README.md` 构建步骤 | `<=8` 步 `(8,10]`；`9-10` `(6,8]`；`11-12` `(4,6]`；`13-14` `(2,4]`；`>14` `[0,2]` |
| 环境安装成功率 | 是否能完成 LLVM + PTOAS 构建与安装 | 真实执行记录 | `100%` `(8,10]`；`80%-99%` `(6,8]`；`60%-79%` `(4,6]`；`40%-59%` `(2,4]`；`<40%` `[0,2]` |

### 环境校验

| 指标 | 在 PTOAS 中怎么测 | 主要证据 | 评分口径 |
| --- | --- | --- | --- |
| 环境安装后校验成功率 | 是否有明确校验动作，例如 `ptoas --version`、Python import、sample compile | `README.md` 5.1/5.2/5.3, `ci.yml` | `100%` `(8,10]`；`80%-99%` `(6,8]`；`60%-79%` `(4,6]`；`40%-59%` `(2,4]`；`<40%` `[0,2]` |

推荐最小校验命令：

```bash
./build/tools/ptoas/ptoas --version
python3 -c "from mlir.dialects import pto; print('ok')"
```

## 易开发、易演进

### 获取示例代码

| 指标 | 在 PTOAS 中怎么测 | 主要证据 | 评分口径 |
| --- | --- | --- | --- |
| 检索命中成功率 | 统计定位到默认样例目录所需轮次 | `test/samples/MatMul/`, `test/samples/Addc/`, `README.md` 5.3 | 1 次 `(8,10]`；2 次 `(6,8]`；3 次 `(4,6]`；4-5 次 `(2,4]`；>5 次 `[0,2]` |

### 运行示例代码

| 指标 | 在 PTOAS 中怎么测 | 主要证据 | 评分口径 |
| --- | --- | --- | --- |
| 示例代码运行耗时 | 从运行 sample 到拿到 `.pto` / `.cpp` / compile-only 结果的时间 | `README.md` 5.3/5.4, `test/samples/MatMul/` | `<=2` 分钟 `(8,10]`；`2-4` `(6,8]`；`4-6` `(4,6]`；`6-8` `(2,4]`；`>8` `[0,2]` |
| 示例代码运行成功率 | `py -> .pto -> .cpp` 或 compile-only / validation 是否成功 | 同上 | `100%` `(8,10]`；`80%-99%` `(6,8]`；`60%-79%` `(4,6]`；`40%-59%` `(2,4]`；`<40%` `[0,2]` |

推荐记录字段：

- 使用的样例路径
- 实际命令
- 是否需要改环境变量
- 首次是否成功
- 报错是否可定位

## 何时记 `N/A`

`01` 场景里一般不应大面积 `N/A`。只有这些情况可以记：

- 当前任务明确不允许实际执行下载/安装/运行，导致只能做文档审阅
- 某项数据必须依赖真实环境，但当前环境完全不具备，且仓库证据不足以推断

此时保留定性结论，但不要伪造数值分数。
